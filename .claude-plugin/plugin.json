{
	"author": {
		"name": "mhaynie"
	},
	"description": "Refuses a write that adds a tombstone comment -- prose about a state the code is no longer in, or an argument aimed at whoever reviews the diff. The refused lines are moved to .git/TOMBSTONES for the commit message instead of deleted",
	"hooks": {
		"PreToolUse": [
			{
				"hooks": [
					{
						"command": "${CLAUDE_PLUGIN_ROOT}/build/no-tombstones",
						"type": "command"
					}
				],
				"matcher": "*"
			}
		]
	},
	"keywords": [
		"comments",
		"documentation",
		"staleness",
		"narration",
		"pretooluse",
		"hook",
		"enforcement"
	],
	"name": "no-tombstones",
	"version": "1265"
}