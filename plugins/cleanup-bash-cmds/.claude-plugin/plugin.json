{
	"$schema": "../../example-plugin/.claude-plugin/plugin.schema.json",
	"name": "cleanup-bash-cmds",
	"description": "Parses Bash commands with shfmt's syntax tree: denies heredocs, strips 2>/dev/null and trailing | head / | tail / | grep / || true / 2>&1, rewrites trailing > file into | tee file, and ensures set -o pipefail on every command",
	"author": {
		"name": "mhaynie"
	},
	"keywords": ["bash", "cleanup", "rewrite", "hook", "stderr", "devnull", "shfmt", "ast", "tee", "pipefail"],
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/hook.sh"
					}
				]
			}
		]
	},
	"mh": {
		"include_in_marketplace": true
	}
}
