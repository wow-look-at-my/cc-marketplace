{
	"$schema": "../../example-plugin/.claude-plugin/plugin.schema.json",
	"name": "cleanup-bash-cmds",
	"description": "Parses Bash commands with shfmt's syntax tree and rewrites them to strip ALL 2>/dev/null stderr suppression and trailing | head / | tail stages, plus unnecessary 2>&1, || true, trailing | grep, and set -e",
	"author": {
		"name": "mhaynie"
	},
	"keywords": ["bash", "cleanup", "rewrite", "hook", "stderr", "devnull", "shfmt", "ast"],
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/hook.sh"
					}
				]
			}
		]
	},
	"mh": {
		"include_in_marketplace": true
	}
}
