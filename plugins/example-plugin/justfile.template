[private]
help:
    @just --list

prebuild:
    # Add commands to run before the build here
    echo "No prebuild step configured"

postbuild:
    # Add commands to run after the build here
    echo "No postbuild step configured"
