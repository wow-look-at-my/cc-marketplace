{
	"$schema": "../../example-plugin/.claude-plugin/plugin.schema.json",
	"name": "detect-permission-seeking",
	"description": "Refuses a stop whose closing message asks permission to keep going, answering with the one word the user would have typed",
	"author": {
		"name": "mhaynie"
	},
	"keywords": ["stop", "permission", "autonomy", "behavior", "hook"],
	"hooks": {
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/build/detect-permission-seeking"
					}
				]
			}
		]
	},
	"mh": {
		"include_in_marketplace": true
	}
}
