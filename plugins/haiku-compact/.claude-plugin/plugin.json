{
	"$schema": "../../example-plugin/.claude-plugin/plugin.schema.json",
	"name": "haiku-compact",
	"description": "Intercepting proxy that swaps the model to Haiku for Claude Code context compaction, cutting compaction cost without changing the model used for normal turns",
	"author": {
		"name": "mhaynie"
	},
	"keywords": ["compact", "haiku", "model", "cost", "proxy"],
	"hooks": {
		"SessionStart": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "${CLAUDE_PLUGIN_ROOT}/build/haiku-compact daemon"
					}
				]
			}
		]
	},
	"mh": {
		"include_in_marketplace": true
	}
}
